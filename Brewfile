# Homebrew casks only. Formulae are declared in mise/config.toml instead,
# because mise's [bootstrap.packages] can install formulae but not casks.

# Set of tools to manage resources and applications hosted on Google Cloud
cask "gcloud-cli"
