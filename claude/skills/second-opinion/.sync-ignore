Presence of this file tells sync-links.sh to skip this skill: its code stays
in the repo but it is not linked into ~/.claude, so Claude Code won't load it.
Delete this file and re-run sync-links.sh to re-enable the skill.
